# Fly.io deployment. Change `app` to a unique name, then:
#   fly launch --no-deploy --copy-config      (first time only)
#   fly secrets set OPENAI_API_KEY=sk-...
#   fly deploy
app = "gridwise-bup-2026"
primary_region = "sin"

[build]
  dockerfile = "Dockerfile"

[env]
  PORT = "8080"
  LOG_LEVEL = "INFO"
  OPENAI_MODEL = "gpt-5.4-mini"
  OPENAI_FALLBACK_MODEL = "gpt-4.1"

[http_service]
  internal_port = 8080
  # false: a judge calling http:// must not get a 301 (some clients turn a redirected POST into GET)
  force_https = false
  # Always on: judges must reach the service during the whole evaluation window, no cold starts.
  auto_stop_machines = "off"
  auto_start_machines = true
  min_machines_running = 1

  [http_service.concurrency]
    type = "requests"
    soft_limit = 40
    hard_limit = 80

  [[http_service.checks]]
    grace_period = "20s"
    interval = "15s"
    method = "GET"
    path = "/health"
    timeout = "5s"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
